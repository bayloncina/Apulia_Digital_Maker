package thread;

import java.util.ArrayList;

public class MyStack {
	
	private ArrayList <Numeri> insiemNum = new ArrayList<Numeri>(20);
	 
	 public synchronized numeri pop ()
	 {
		 int num;
			while (buffer.size() == 0) {
				try {
					this.wait();
				} catch (InterruptedException e) {
					// ignore it...
				}
			}
			c = ((Numeri) insiemNum.remove(buffer.size() - 1)).charValue();
			return num;

		 
	 }
	 

	 public synchronized numeri push (int numero)
	 {
		 
	 }
}