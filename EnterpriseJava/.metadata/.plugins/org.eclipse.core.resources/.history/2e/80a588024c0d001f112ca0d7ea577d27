package strutturedati;
import java.util.Comparator;
public class Comparatore implements Comparator<>{


	@Override
	public int compare(Regione regione1, Capoluogo capoluogo1) {
		int compareResult = regione1.getRegione.compareTo(capoluogo1.getCapoluogo());
		return compareResult;
	}}
