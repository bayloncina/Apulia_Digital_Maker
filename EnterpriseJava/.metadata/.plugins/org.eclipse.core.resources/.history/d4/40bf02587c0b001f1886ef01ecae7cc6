package thread;
import java.util.ArrayList;

public class MyStack {
	
	private ArrayList <Integer> insiemNum = new ArrayList<Integer>(20);
	 
	 public synchronized pop ()
	 {
		 int num;
			while (buffer.size() == 0) {
				try {
					this.wait();
				} catch (InterruptedException e) {
					// ignore it...
				}
			}
			int c = ((NumeriPari) insiemNum.remove(buffer.size() - 1));
			return num;

		 
	 }
	 

	 public synchronized numeri push (int numero)
	 {
		 
	 }
}